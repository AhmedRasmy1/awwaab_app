name: awwaab_app
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  cached_network_image: ^3.4.1

  cupertino_icons: ^1.0.8

  # Networking
  dio: ^5.9.0
  flutter:
    sdk: flutter

  # State Management & DI
  flutter_bloc: ^9.1.1

  flutter_localizations:
    sdk: flutter

  # Splash
  flutter_native_splash: ^2.4.7
  flutter_svg: ^2.2.3
  font_awesome_flutter: ^10.12.0
  get_it: ^9.2.0
  injectable: ^2.7.1+2
  pretty_dio_logger: ^1.4.0
  retrofit: ^4.9.1
  shared_preferences: ^2.5.4
  url_launcher: ^6.3.2
dependency_overrides:
  analyzer: ^6.4.1
dev_dependencies:
  build_runner: ^2.4.8
  flutter_launcher_icons: ^0.14.4
  flutter_lints: ^4.0.0  # رجعتها 4 عشان التوافق
  flutter_test:
    sdk: flutter
  injectable_generator: ^2.6.2
  json_serializable: ^6.8.0   # <-- تعديل مهم
  retrofit_generator: ^8.1.0  # <-- تعديل مهم جداً
  # analyzer: ^9.0.0          <-- اتأكد إن السطر ده ممسوح
flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/fonts/
